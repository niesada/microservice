spring:
  data:
    mongodb:
      uri: mongodb+srv://rzy119:rzy1982@cluster0.b5zjast.mongodb.net/product-service
  application:
    name: product-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:password@localhost:8761/eureka

server:
  port: 0

# Actuator Prometheus Endpoint
management:
  endpoints:
    web:
      exposure:
        include: prometheus